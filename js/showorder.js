var fs = require('fs');

function showOrder(){
    window.alert('showOrder');
    // var fs = require('fs');
    window.alert('fs=');
// var imgPath =__dirname + '/uploads' + '/';
var imgPath ='/uploads/0918999000.jpg';
var jsonFile = __dirname + '/data/order.json';
window.alert('imgPath ='+imgPath);
// fs.readFile(imgPath + phone + '.jpg', buffer, function (err) {
//   document["img"].src = imgPath;
  // fs.readFile(jsonFile, function (err, content) {

  // });
// });
  }